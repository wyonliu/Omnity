[build-system]
requires = ["hatchling>=1.24.0"]
build-backend = "hatchling.build"

[project]
name = "omnity-soap"
version = "0.1.0"
description = "SOAP — Spatial Omnity Agentic Protocol (spec validation + soap-mcp)"
readme = "README.md"
requires-python = ">=3.9"
license = "Apache-2.0"
dependencies = [
  "jsonschema>=4.20.0",
  "referencing>=0.35.0",
]

[project.optional-dependencies]
# Official MCP SDK targets Python 3.10+; core SOAP tooling runs on 3.9.
mcp = ["mcp>=1.2.0; python_version>='3.10'"]
dev = ["pytest>=8.0"]

[project.scripts]
soap-validate = "omnity_soap.cli:main"
soap-mcp = "omnity_soap.mcp_server:main"

[tool.hatch.build.targets.wheel]
packages = ["src/omnity_soap"]

[tool.hatch.build.targets.wheel.force-include]
"spec/schemas" = "omnity_soap/schemas"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
