name: Mindos CI

on:
  push:
    paths:
      - "packages/mindos/**"
      - ".github/workflows/mindos-ci.yml"
  pull_request:
    paths:
      - "packages/mindos/**"
      - ".github/workflows/mindos-ci.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.11", "3.12"]
    defaults:
      run:
        working-directory: packages/mindos

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package (dev)
        run: pip install -e ".[dev]"

      - name: Pytest
        run: pytest -q --tb=short

      - name: Build wheel
        if: matrix.python-version == '3.12'
        run: |
          pip install build
          python -m build

  publish:
    needs: test
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/mindos-v')
    runs-on: ubuntu-latest
    permissions:
      id-token: write  # PyPI trusted publishing
    defaults:
      run:
        working-directory: packages/mindos

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build
        run: |
          pip install build
          python -m build

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: packages/mindos/dist/
